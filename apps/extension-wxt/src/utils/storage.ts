// export const storage  = {
//     defineItem: <T>(key: string, options: { defaultValue: T }) => {
//         const item = browser.storage.sync.get(key);
//         if (item) {
//             return JSON.parse(item) as T;
//         } else {
//             const { defaultValue } = options;
// }
